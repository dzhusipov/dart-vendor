class Contact {
  String phone_number;
  String first_name;
  String last_name;
  int user_id;

  Contact(
      {required this.phone_number,
      required this.first_name,
      required this.last_name,
      required this.user_id});
}
